#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    //printf("%d",atoi("123"));
    int x,y;
    x=atoi("124"); //atoi,atol,atof
    y=x+5;
    printf("%d",y);
    return 0;
}
